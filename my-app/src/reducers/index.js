

const initialState = {
    isLoading: false,
    error: '',
    character: '',
    image: '',
    name: '',
    amiiboSeries: ''
}

export const reducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}